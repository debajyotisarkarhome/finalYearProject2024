const host = "http://4.240.76.144:8080";
const registerRoute = `${host}/register`;
const loginRoute = `${host}/login`;
const addDeviceRoute = `${host}/createDevice`;
const getDeviceList = `${host}/getDeviceList`;
const getDeviceLogs = `${host}/getDeviceLogs`;

export {
  host,
  registerRoute,
  loginRoute,
  addDeviceRoute,
  getDeviceList,
  getDeviceLogs,
};
